===============================
  🔐 SecretStore — Инструкция
===============================

📦 Состав архива:
-------------------------------
project/
├─ WPFApp/
│  └─ Debug/                  ← готовая сборка приложения (SecretStore.exe)
│
├─ sql/
│  └─ init.sql                ← создание таблиц PostgreSQL
│
├─ docker-compose.yml         ← запуск PostgreSQL + OpenBao
├─ start.bat                  ← запуск всех сервисов и приложения
└─ README.txt                 ← этот файл


🚀 Запуск проекта:
-------------------------------
1️⃣ Установите:
   - Docker Desktop (или Docker Engine)
   - .NET Desktop Runtime (если не установлен)

2️⃣ Распакуйте архив проекта в удобное место, например:
   D:\SecretStoreProject

3️⃣ Откройте PowerShell или CMD **от имени администратора**.

4️⃣ Перейдите в папку проекта:
   cd D:\SecretStoreProject

5️⃣ Выполните команду:
   start.bat

⚙️ Что произойдёт:
-------------------------------
✅ Docker автоматически:
   - запустит контейнер PostgreSQL на порту 5434,
   - создаст базу данных и таблицы (init.sql),
   - поднимет OpenBao (Vault) на http://127.0.0.1:8200,
   - установит переменную окружения OPENBAO_TOKEN=root.

✅ После инициализации автоматически запустится приложение:
   WPFApp\Debug\SecretStore.exe

💡 Приложение подключается к OpenBao и PostgreSQL автоматически
по сохранённым параметрам (host, port, dbname, username, password, token).


🧩 Проверка работы:
-------------------------------
1. Дождитесь, пока Docker завершит запуск (примерно 10 секунд).
2. Приложение SecretStore откроется автоматически.
3. Введите мастер-пароль (при первом запуске создаётся новый).
4. После входа появится список секретов (если есть).
5. Можно добавлять, искать и просматривать пароли.

🔍 Проверка контейнеров:
-------------------------------
Чтобы убедиться, что всё запущено:
   docker ps

Должны быть контейнеры:
   - post (PostgreSQL)
   - openbao (Vault)

Для остановки:
   docker-compose down


📘 Контакты и отладка:
-------------------------------
Если приложение не подключается к Vault:
   - откройте http://127.0.0.1:8200
   - проверьте, что токен root активен.

Если не запускается база:
   - убедитесь, что порт 5434 свободен,
   - удалите старые контейнеры командой:
       docker rm -f post openbao

===============================
✅ Проект готов к использованию
Запускается одной командой:  start.bat
===============================
