<Window 
    x:Class="SecretStore.MasterWindowPassword"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    WindowStyle="None"
    WindowStartupLocation="CenterScreen"
    ResizeMode="CanResize"
    Height="260" Width="420"
    Background="#1E1E1E">

    <Grid Margin="18" Background="{StaticResource PanelBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,16">
            <TextBlock Text="🔒 Secret Store" 
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource AccentBrush}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Введите мастер-пароль" 
                       FontSize="13"
                       Foreground="{StaticResource TextDimBrush}"
                       Margin="0,6,0,0"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <PasswordBox x:Name="MasterPasswordBox" Width="280"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
            <Button x:Name="LoginBtn" Content="Войти" Width="120" Margin="6" Click="OkButton_Click"/>
            <Button x:Name="ExitBtn" Content="Выход" Width="120" Margin="6" Click="ExitBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>